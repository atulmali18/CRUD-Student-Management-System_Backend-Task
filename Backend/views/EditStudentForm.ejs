<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Student</title>
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
  >
</head>
<body class="bg-light">
  <div class="container mt-5">
    <div class="card shadow-lg">
      <div class="card-header bg-warning text-dark">
        <h3 class="mb-0">Edit Student Record</h3>
      </div>
      <div class="card-body">
        <form action="/update/<%= student.id %>" method="POST">
          <div class="mb-3">
            <label for="fullName" class="form-label">Full Name</label>
            <input
              type="text"
              name="fullName"
              id="fullName"
              value="<%= student.fullName %>"
              class="form-control"
              required
            >
          </div>

          <div class="mb-3">
            <label for="email" class="form-label">Email</label>
            <input
              type="email"
              name="email"
              id="email"
              value="<%= student.email %>"
              class="form-control"
              required
            >
          </div>

          <div class="mb-3">
            <label for="contact" class="form-label">Contact Number</label>
            <input
              type="number"
              name="contact"
              id="contact"
              value="<%= student.contactNumber %>"
              class="form-control"
              required
            >
          </div>

          <div class="mb-3">
            <label for="course" class="form-label">Course</label>
            <select name="course" id="course" class="form-select" required>
              <option value="HTML" <%= student.course === "HTML" ? "selected" : "" %>>HTML</option>
              <option value="CSS" <%= student.course === "CSS" ? "selected" : "" %>>CSS</option>
              <option value="JavaScript" <%= student.course === "JavaScript" ? "selected" : "" %>>JavaScript</option>
              <option value="React" <%= student.course === "React" ? "selected" : "" %>>React</option>
              <option value="Node" <%= student.course === "Node" ? "selected" : "" %>>Node</option>
            </select>
          </div>

          <div class="mb-3">
            <label for="batchCode" class="form-label">Batch Code</label>
            <input
              type="text"
              name="batchCode"
              id="batchCode"
              value="<%= student.batchCode %>"
              class="form-control"
              required
            >
          </div>

          <div class="mb-3">
            <label for="joiningDate" class="form-label">Joining Date</label>
            <input
              type="date"
              name="joiningDate"
              id="joiningDate"
              value="<%= student.joiningDate %>"
              class="form-control"
              required
            >
          </div>

          <div class="mb-3">
            <label for="remarks" class="form-label">Remarks</label>
            <textarea
              name="remarks"
              id="remarks"
              rows="3"
              class="form-control"
            ><%= student.remarks %></textarea>
          </div>

          <div class="d-flex justify-content-between">
            <a href="/" class="btn btn-secondary">Cancel</a>
            <button type="submit" class="btn btn-primary">Update</button>
          </div>
        </form>
      </div>
    </div>
  </div>

  <script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js">
  </script>
</body>
</html>
